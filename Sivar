-- Rayfield 라이브러리 로드
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Window 생성 (에임 TP 전용 핵)
local Window = Rayfield:CreateWindow({
    Name = "에임 TP 핵",
    LoadingTitle = "에임 TP 핵",
    LoadingSubtitle = "Z 키로 순간이동",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "AimTPHack",
        FileName = "Config"
    },
    Discord = {
        Enabled = false,
        Invite = "",
        RememberJoins = true
    },
    KeySystem = false
})

-- 메인 탭 생성
local MainTab = Window:CreateTab("메인", "home")

-- 에임 TP 관련 변수
local aimTPEnabled = false

-- 텔레포트 함수
local function teleportToMouse()
    local player = game.Players.LocalPlayer
    local character = player.Character
    if not character then return false end
    
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if not rootPart then return false end
    
    local mouse = player:GetMouse()
    if not mouse or not mouse.Hit then return false end
    
    -- 마우스 위치로 순간이동
    rootPart.CFrame = CFrame.new(mouse.Hit.p)
    return true
end

-- Z 키 입력 처리
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Z and aimTPEnabled then
        if teleportToMouse() then
            Rayfield:Notify({
                Title = "에임 TP",
                Content = "마우스 위치로 순간이동했습니다",
                Duration = 1
            })
        else
            Rayfield:Notify({
                Title = "에임 TP",
                Content = "순간이동 실패 (캐릭터 없음)",
                Duration = 1.5
            })
        end
    end
end)

-- 메인 탭 UI
MainTab:CreateSection("에임 TP 설정")

-- 에임 TP 토글
local aimTPToggle = MainTab:CreateToggle({
    Name = "에임 TP",
    CurrentValue = false,
    Flag = "AimTPToggle",
    Callback = function(Value)
        aimTPEnabled = Value
        if Value then
            Rayfield:Notify({
                Title = "에임 TP",
                Content = "활성화되었습니다. Z 키를 눌러 순간이동하세요",
                Duration = 2
            })
        else
            Rayfield:Notify({
                Title = "에임 TP",
                Content = "비활성화되었습니다",
                Duration = 1.5
            })
        end
    end
})

-- 사용법 안내
MainTab:CreateLabel("")
MainTab:CreateLabel("📌 사용 방법")
MainTab:CreateLabel("1. '에임 TP' 토글을 켜세요")
MainTab:CreateLabel("2. Z 키를 누르면 마우스 위치로 순간이동")
MainTab:CreateLabel("")
MainTab:CreateLabel("⚠️ 주의: 벽 너머도 순간이동 가능")

-- 초기화 완료 알림
Rayfield:Notify({
    Title = "에임 TP 핵",
    Content = "로드 완료! Z 키로 순간이동",
    Duration = 3
})
